#ifndef CONFIG_PRIVATE_H
#define CONFIG_PRIVATE_H

// MQTT details
static const char* mqttBroker = "broker.hivemq.com";
static const int mqttPort = 1883;

static const char* mqttCert = "";
static const int mqttCertLen = 1358;

static const char* topicExec = "GsmClientTest/exec";
static const char* topicService = "GsmClientTest/service";
static const char* topicExecOutput = "GsmClientTest/execOutput";

#define MQTT_CONNECTION_AT_COMMAND "AT+CMQTTCONNECT=0,\"tcp://example.com:8883\",60,1,\"username\",\"password\""

#define MQTT_AUTH_CLIENT_ID "test"
#define MQTT_AUTH_USER "username"
#define MQTT_AUTH_PASSWORD "password"

#endif